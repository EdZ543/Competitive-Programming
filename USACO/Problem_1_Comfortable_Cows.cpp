#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int N; cin >> N;
    int addedCows = 0;
    vector<pair<int, int>> cows;

    for (int i = 0; i < N; i++) {
        pair<int, int> cow;
        cin >> cow.first >> cow.second;
    }
}